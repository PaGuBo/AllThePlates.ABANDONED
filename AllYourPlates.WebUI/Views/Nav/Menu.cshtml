@model IEnumerable<string>
@{
    Layout = null;
}
@Html.ActionLink("Home", "Index", "Home")
@*
@foreach (var link in Model)
{
    @Html.RouteLink(link,
    new {
        controller = "User",
        action = "List",
        userName = link,
        page = 1
    },
    new {
        @class = link == ViewBag.userName ? "selected" : null
    })
}
*@